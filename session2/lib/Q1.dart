/*Exercise 1: Dot Operator
Objective: Use the dot operator with a variable.
Instructions:
- Create two variables, length (int) and width (int), and assign them values.
- Calculate the area of a rectangle using the formula area = length * width and print it.
*/
void main() {
  int length = 11;
  int width = 11;
  int area = length * width;
  print(area);
}
